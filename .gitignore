/.phpunit.cache
/node_modules
/vendor
/cache

# Uploaded media is user data, not source. The folder itself is tracked
# via uploads/.gitignore so the app has somewhere to write on a fresh clone.
/uploads/*
!/uploads/.gitignore
!/uploads/.htaccess

/storage/*.key
/storage/pail
.env
.env.backup
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
/.freebuff/
